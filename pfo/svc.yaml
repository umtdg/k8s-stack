apiVersion: v1
kind: Service
metadata:
  name: pfo
  labels:
    app: pfo
spec:
  type: NodePort
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 31070
  selector:
    app: pfo
